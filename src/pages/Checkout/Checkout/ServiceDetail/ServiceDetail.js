import React from "react";

const ServiceDetail = (props) => {
   const services =  props?.serviceName;
    // console.log(services);
  return (
    <div>
      {/* <h3>{services.map(service => {
        <h3>{service}</h3>
      })} </h3> */}
    </div>
  );
};

export default ServiceDetail;
import React from "react";

const ServiceDetail = ({single_service}) => {
    // console.log(single_service);
    // console.log(service);
  return (
    <div>
      <h3>{single_service}</h3>
    </div>
  );
};

export default ServiceDetail;
